# build artifacts
/target/
/*/target/

# data generated during runtime
data/
crack-core/data/
/logs/
scratch

# replit
.cargo/git/
.cargo/registry/
.cargo/

# configuration / secrets
Secrets.toml
.env
.env.old
.env.fish
cracktunes.env
cracktunes.toml
dotenv
dist-manifest.json

# IDE config
#.vscode/

# code coverage
cobertura.xml
**/*.profraw

# subagent-driven-development scratch: ledger, briefs, reports, review packages.
# Working notes for executing a plan, not part of what ships. The plan and spec
# under docs/superpowers/ stay tracked -- those are the durable documents.
.superpowers/

# git worktrees created for parallel work; never part of the tree
.worktrees/
